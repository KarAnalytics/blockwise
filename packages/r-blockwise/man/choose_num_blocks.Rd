% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blocks.R
\name{choose_num_blocks}
\alias{choose_num_blocks}
\title{Estimate the number of blocks via the elbow heuristic}
\usage{
choose_num_blocks(X, low_threshold = 0.05, n_restarts = 10, k_max = NULL)
}
\arguments{
\item{X}{A data.frame of predictors; may contain \code{NA}.}

\item{low_threshold}{Fraction below which a column is considered absent in
a candidate subset. Default 0.05.}

\item{n_restarts}{Number of k-means restarts per k. Default 10.}

\item{k_max}{Upper bound on k. Default \code{min(ncol(X), 50)}.}
}
\value{
A list with \code{n_blocks} (the chosen k) and \code{missing_curve}
  (numeric vector of length \code{k_max}).
}
\description{
Applies k-means to the binary missingness-indicator matrix for
k = 1, ..., \code{k_max} and records, for each k, the fraction of
still-missing cells after dropping columns that are sparse within each
cluster. The curve is monotone-decreasing; BRM picks the k at its elbow.
}
\details{
Typically called internally by \code{\link{brm}} when \code{n_blocks = NULL}.
}
\references{
Srinivasan, K., Currim, F., Ram, S. (2025). A Reduced Modeling
  Approach for Making Predictions With Incomplete Data Having Blockwise
  Missing Patterns. \emph{INFORMS Journal on Data Science}.
}
