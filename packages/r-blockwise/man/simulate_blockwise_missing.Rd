% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_blockwise_missing}
\alias{simulate_blockwise_missing}
\title{Simulate a blockwise missing pattern on otherwise complete data}
\usage{
simulate_blockwise_missing(
  data,
  blocks,
  prop_missing,
  noise = 0.05,
  seed = 1234L
)
}
\arguments{
\item{data}{A data.frame.}

\item{blocks}{A list of character vectors; each vector names the columns
masked jointly for one block of rows.}

\item{prop_missing}{Proportion of rows affected per block. Either a scalar
(applied to each block) or a numeric vector of length
\code{length(blocks)}.}

\item{noise}{Extra per-column random-NA rate applied on top, restricted to
columns named in any block. Default 0.05. Set to 0 to disable.}

\item{seed}{Optional integer base seed. Default 1234.}
}
\value{
A data.frame of the same shape as \code{data}, with \code{NA}s
  introduced in the specified pattern.
}
\description{
Joint-masks groups of columns on randomly chosen rows, optionally adding
light column-wise random-NA noise on top. Useful for benchmarking BRM
on complete datasets; the default arguments reproduce the simulation
design used in Srinivasan, Currim, and Ram (2025).
}
\examples{
df <- data.frame(a = 1:100, b = 1:100, c = 1:100, d = 1:100)
simulate_blockwise_missing(df,
  blocks       = list(c("a", "b"), c("c", "d")),
  prop_missing = 0.3)
}
